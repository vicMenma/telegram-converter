# ── Telegram client (MTProto — no 50 MB limit) ────────────────────
pyrogram==2.0.106
tgcrypto==1.2.5

# ── Environment variables ─────────────────────────────────────────
python-dotenv==1.0.1

# ── Async file I/O and HTTP ───────────────────────────────────────
aiofiles==24.1.0
aiohttp==3.10.10

# ── Video downloading (YouTube, Twitter, TikTok, etc.) ───────────
yt-dlp==2024.11.18

# ── System stats for /stats command ──────────────────────────────
psutil==6.1.1

# ── NOTE: FFmpeg must be installed separately ─────────────────────
#   Dockerfile handles this automatically
#   libtorrent is optional — only needed for magnet links
